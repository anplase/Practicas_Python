"""cuando el programa se va haciendo muy grande lo ideal es hacer modulos del programa,
y esto es ir trozeando el programa como lo vamos a hacer con el archivo main y el archivo calculadora.py

los dos archivos deben de estar al mismo nivel guardados

cuando llamamos a un archivo desde otro, se crea una carpeta __pycache__ que tiene un 
archivo con la extensión .pyc que es un archivo compilado por si no encuentra, y a su vez si python
encuentra ese archivo compilado lo va a usar y se va a volver mas rapido en la ejecucion.



"""